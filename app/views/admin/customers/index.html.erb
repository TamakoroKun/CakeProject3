<h2>会員一覧</h2>

<h2>配送先登録 / 一覧</h2>

<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th>会員ID</th>
      <th>氏名</th>
      <th>メールアドレス</th>
      <th>ステータス</th>
    </tr>
  </thead>

  <tbody>
    <% @customers.each do |customer| %>
      <tr>
        <td><%= customer.id %></td>
        <td><%#=   %>
          <%= customer.first_name %></td>
        <td><%= customer.email %></td>
        <td class="edit-btn"><%= link_to "編集する", edit_address_path(address) %></td>
      </tr>
    <% end %>
  </tbody>
</table>